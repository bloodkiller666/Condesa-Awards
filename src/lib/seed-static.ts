// Este archivo contiene los datos "estáticos" de ejemplo (fallback) usados
// cuando no hay datos en Supabase. En producción, los datos provienen de la DB.
// Se mantiene separado de `data.ts` para que las categorías reales vivan en la DB.
export {};